<controls:MicaWindow x:Class="AdvanceClip.Windows.PdfMergeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:MicaWPF.Controls;assembly=MicaWPF"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="PDF Merge" Height="500" Width="600"
        WindowStartupLocation="CenterScreen"
        SystemBackdropType="Mica" Background="Transparent" Topmost="True">
    
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" ResizeBorderThickness="6" CornerRadius="8" GlassFrameThickness="-1" />
    </WindowChrome.WindowChrome>

    <Grid Margin="16,8,16,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16" Orientation="Horizontal" WindowChrome.IsHitTestVisibleInChrome="False">
            <ui:SymbolIcon Symbol="DocumentPdf24" Foreground="#EF4444" FontSize="24" Margin="0,0,12,0" />
            <TextBlock Text="Merge PDFs" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource MicaWPF.Brushes.TextFillColorPrimary}" VerticalAlignment="Center"/>
        </StackPanel>

        <TextBlock Grid.Row="1" Text="Arrange the selected PDFs in the exact order you want them merged. Drag items or use buttons." FontSize="13" Opacity="0.7" Foreground="{DynamicResource MicaWPF.Brushes.TextFillColorSecondary}" Margin="0,0,0,12" TextWrapping="Wrap" />

        <Border Grid.Row="2" Background="#08FFFFFF" BorderBrush="#12FFFFFF" BorderThickness="1" CornerRadius="8" Padding="2">
            <ListBox x:Name="PdfItemsList" Background="Transparent" BorderThickness="0" Margin="4"
                     ScrollViewer.VerticalScrollBarVisibility="Auto" AllowDrop="True">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0,2"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                    <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderThickness="0" CornerRadius="6" Padding="8,10">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#1AFFFFFF"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#0AFFFFFF"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            
                            <!-- Index/Icon -->
                            <Border Grid.Column="0" Width="36" Height="36" CornerRadius="4" Background="#11FFFFFF" Margin="0,0,12,0">
                                <ui:SymbolIcon Symbol="DocumentPdf24" Foreground="#EF4444" FontSize="20" />
                            </Border>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,0,12,0">
                                <TextBlock Text="{Binding FileName}" FontSize="14" FontWeight="Medium" Foreground="{DynamicResource MicaWPF.Brushes.TextFillColorPrimary}" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding FormattedSize}" FontSize="11" Foreground="{DynamicResource MicaWPF.Brushes.TextFillColorTertiary}" Margin="0,2,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                <ui:Button Appearance="Transparent" Tag="{Binding}" Click="MoveUp_Click" ToolTip="Move Up" Margin="0,0,4,0">
                                    <ui:Button.Icon><ui:SymbolIcon Symbol="ChevronUp24" /></ui:Button.Icon>
                                </ui:Button>
                                <ui:Button Appearance="Transparent" Tag="{Binding}" Click="MoveDown_Click" ToolTip="Move Down">
                                    <ui:Button.Icon><ui:SymbolIcon Symbol="ChevronDown24" /></ui:Button.Icon>
                                </ui:Button>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <Grid Grid.Row="3" Margin="0,16,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button Content="Cancel" Appearance="Secondary" Margin="0,0,12,0" Padding="20,8" Click="Cancel_Click" />
                <ui:Button x:Name="MergeBtn" Content="Merge PDFs" Appearance="Primary" Padding="20,8" Click="Merge_Click">
                    <ui:Button.Icon><ui:SymbolIcon Symbol="DocumentCopy24" /></ui:Button.Icon>
                </ui:Button>
            </StackPanel>
        </Grid>
    </Grid>
</controls:MicaWindow>
